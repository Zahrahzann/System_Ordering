<IfModule mod_rewrite.c>
    RewriteEngine On

    # Arah ke semua request
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # ... ke index.php
    RewriteRule ^ index.php [QSA,L]
</IfModule>